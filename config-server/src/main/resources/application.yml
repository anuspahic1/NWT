server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///C:/Users/Adna/Downloads/config-repo

          default-label: master #default branch
          force-pull: true